[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
  'cd /app/frontend && npm ci',
  'cd /app/backend && npm ci'
]

[phases.build]
cmds = ['cd /app/frontend && npm run build']

[start]
cmd = 'cd /app/backend && node server.js'